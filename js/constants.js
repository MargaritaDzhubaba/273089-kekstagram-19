'use strict';

// constants.js
(function () {
  var ESC_KEY = 'Escape';
  var ENTER_KEY = 'Enter';
  var DEFAULT_FILTER = 'none';

  window.constants = {
    ESC_KEY: ESC_KEY,
    ENTER_KEY: ENTER_KEY,
    DEFAULT_FILTER: DEFAULT_FILTER
  };
})();
